<article class="post-card">
    {{#if feature_image}}
    <a href={{url}}>
        <img src={{img_url feature_image}} alt={{name}}>
    </a>
    {{/if}}
    <h2>{{title}}</h2>
    <p>{{excerpt}}</p>
    <a href={{url}} class="button">See more</a>
</article>